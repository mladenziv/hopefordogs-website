.DS_Store
node_modules/
.claude/
.agents/
.cursor/
*.sql
*.jsonl

# Mollie live API key — set directly on the server, never commit
api/mollie/config.php

# Supabase service_role key — set on server, never commit
api/social-media/config.php

# Anthropic API key (OCR/vision) — set on server, never commit
api/ocr/config.php

# Manual backups (pre-edit copies)
/backups/

# SEO audit working folders — local analysis artifacts, never deploy to the public site
/seo-audit-third-pass/
